version: "3.7"

services:
  item-app:
    image: localhost:5000/items-app
    ports:
      - 80:8080
    volumes:
      - ./:/app
  item-db: 
    image: localhost:5000/items-db
    volumes:
      - app-db:/data/db
volumes:
  app-db: